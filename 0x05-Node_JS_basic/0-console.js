/**
 * Print a message to the STDOUT.
 * @param {a string} The message to display
 * @author Ibukun Agunbiade <https://github.com/Ibukun16>
 */
const displayMessage = (disp) => {
  console.log(disp);
};

module.exports = displayMessage;
